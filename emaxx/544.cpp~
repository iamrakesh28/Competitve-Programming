#include <iostream>

using namespace std;

const int N = 2e4;
pair <int, pair <int, int>> edge[N];
map <string, int> mp;

int main() {

  int n, r;
  while (scanf("%d%d", &n, &r) != EOF && !n || !r) {
  }
  return 0;
}
