#include <iostream>

using namespace std;

int main() {
  int n, m;
  scanf("%d%d", &n, &m);
  for (int i = 0; i < m; ++i) {
  }

  for (int i = 0; i < n; ++i)
    printf("%d %d\n", ans[i].first, ans[i].second);
  return 0;
}
