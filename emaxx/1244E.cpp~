#include <iostream>

typdef long long ll;
using namespace std;

const int N = 1e5;
int arr[N];

int main() {
  int n;
  ll k;
  scanf("%d%lld", &n, &k);
  for (int i = 0; i < n; ++i)
    scanf("%d", arr + i);
}
