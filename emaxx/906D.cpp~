#include <iostream>

using namespace std;

int phi(int n) {
  int result = n;
  for (int i = 2; i * i <= n; ++i) {
    if (n % i)
      continue;
    result -= result / i;
    while (n % i == 0)
      n /= i;
  }
  if (n > 1)
    result -= result / n;
  return result;
}

int main() {
  int m = 1e9 + 7;
  for (int i = 0; i < 25; ++i) {
    m = phi(m);
    cout<<m<<endl;
  }
  return 0;
}
