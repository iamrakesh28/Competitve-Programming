#include <iostream>

using namespace std;

const int N = 1e5;
int A[N], alph[N];

int main() {
  int q;
  scanf("%d", &q);
  for (int i = 0; i < q; ++i) {
    int n, s;
    scanf("%d%d", &n, &s);
    for (int j = 0; j < n; ++j)
      scanf("%d", A + j);
  }
  return 0;
}
