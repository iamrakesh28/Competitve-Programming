#include <iostream>
#include <vector>

using namespace std;

const int N = 1e5;
int arr[N], st[N][18];

int main() {

  int n;
  scanf("%d", &n);
  for (int i = 0; i < n; ++i)
    scanf("%d", arr + i), st[i][0] = arr[i];
  for (int i = 1; i < 18; ++i)
    for (int j = 0; j + (1 << i) <= n; ++j)
      st[j][i] = __gcd(st[j][i - 1], st[j + (1 << (i - 1))][i - 1]);
  return 0;
}
