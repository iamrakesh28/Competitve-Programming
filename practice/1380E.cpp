#include <iostream>
#include <set>

using namespace std;

const int N = 2e5 + 5;
int disc[N];

int main() {
  int m, n;
  scanf("%d%d", &n, &m);
  for (int i = 0; i < n; ++i)
    scanf("%d", disc + i);
  return 0;
}
