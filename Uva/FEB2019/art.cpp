#include <bits/stdc++.h>

using namespace std;

char S[1000005];
int alpa[26];

int solve(int sum)
{
	
}
int main()
{
	int q,sum;
	char ch;
	scanf("%d",&q);
	ch = getchar_unlocked();
	while(q--)
	{
		memset(aplha,0,sizeof int);
		ch = getchar_unlocked();
		sum = 0;
		while(ch != '\n')
		{
			alpha[ch-'A']++;
			sum++;
			ch = getchar_unlocked();
		}
		
	}
	return 0;
}
