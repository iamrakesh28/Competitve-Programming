#include <bits/stdc++.h>

using namespace std;

int a,b,c,d,e,f;
int change(int val)
{
	if(val == 0)
	return 0;
	int minC;
	if(val-5 < 0)	
}
int main()
{
	float val;
	while(scanf("%d%d%d%d%d%d%f",&a,&b,&c,&d,&e,&f,&val) != EOF && a && b && c && d && e &&f)
	{
		
	}
	return 0;
}
