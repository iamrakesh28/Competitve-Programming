#include <isotream>
#include <set>

using namespace std;
const int N = 1e5 + 5;
int x[N], y[N];
int n, m, sx, sy, fx, fy;

void bfs(void) {
  set <pair <int, int>> sx;
  for () {
  }
}

int main() {
  scanf("%d%d", &n, &m);
  scanf("%d%d%d%d", &sx, &sy, &fx, &fy);
  for (int i = 0; i < n; ++i)
    scanf("%d%d", x + i, y + i);
  return 0;
}
