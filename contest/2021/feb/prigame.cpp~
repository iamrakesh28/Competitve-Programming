#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

const int N = 1e6;
vector <int> primes;

void find_primes(int n) {

  primes.push_back(2);
  for (int i = 3; i <= n; i += 2) {

    bool factor = false;
    for (int j = 0; j < primes.size() && primes[j] * primes[j] <= i; ++j) {
    }
  }

  return;
}
