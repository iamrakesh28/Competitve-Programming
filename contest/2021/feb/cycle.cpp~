#include <iostream>
#include <vector>
#include <stack>

using namespace std;

const int N = 1e5 + 5;
bool visit[N];

vector <int> idsOfAlumni (vector <int> &alumniID) {

  int n = alumniID.size();
  memset(visit, false, sizeof(bool) * n);
}
