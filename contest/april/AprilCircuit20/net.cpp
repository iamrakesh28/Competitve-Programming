#include <iostream>
#include <vector>

typedef long long ll;
using namespace std;

const int N = 5e3 + 5, prime = 1e9 + 7;
const int mod = 1e8 + 7;

struct Point {
  int x, y, v, id;
} point[N];

ll dp[N][3];
vector <int> cur, prev;

int main() {
  int n, m, k, w;
  return 0;
}
