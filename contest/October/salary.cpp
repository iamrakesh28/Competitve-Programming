#include <iostream>

using namespace std;

const int N = 2e5;
pair <int, int> ind[N];

long long solve(int n, long long med) {
	if (ind[n / 2 + 1])
	long long cost = 0;
	for (int) {
	}
}

int main() {

	int t;
	scanf("%d", &t);
	while (t--) {
		int n;
		long long s;
		scanf("%d%lld", &n, &s);
		for (int i = 0; i < n; ++i)
			scanf("%d%d", &ind[i].first, &ind[i].second);
	}
	return 0;
}
