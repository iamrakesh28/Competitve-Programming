#include <iostream>
#include <cstring>

using namespace std;

const int N = 1e5 + 5;
char s[N];

int main() {
  int t;
  scanf("%d", &t);

  while (t--) {
    int n, cnt = 0;
    scanf("%s", s);

    n = strlen(s);
    for (int i = 0; i < n; ++i) {
      if ()
    }

    printf("%d\n", cnt);
  }
  
  return 0;
}
