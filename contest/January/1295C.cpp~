#include <iostream>

using namespace std;

const int N = 1e5 + 5;
char s[N], t[N];

int main() {
  set <int> s;
  s.insert(1);
  s.insert(3);
  int a = *s.lower_bound(2);
  cout<<a<<endl;
  return 0;
  int q;
  scanf("%d", &q);
  while (q--) {
  }
  return 0;
}
