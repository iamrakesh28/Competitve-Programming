#include <iostream>

using namespace std;

const int N = 2;
int mat[N][N];

void print(int n) {
  for (int i = 0; i < n; ++i) {
    for (int j = 0; j < n; ++j)
      printf("%d ", mat[i][j]);
    puts("");
  }
}
bool brute(int i, int j, int n) {
  if (i >= n) {
    print(n);
    return true;
  }
}
