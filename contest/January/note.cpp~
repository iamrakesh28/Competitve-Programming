#include <iostream>

using namespace std;

int main() {

  int n, one = 0, win = 1;
  scanf("%d", &n);
  for (int i = 0; i < n; ++i) {
    int ai;
    scanf("%d", &ai);
    if (ai == 1)
      one++;
  }
  if (n == 1) {
    puts("2");
    return 0;
  }
  return 0;
}
